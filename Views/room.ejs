<!DOCTYPE html>
<html lang="en">

<!-- All icons -->
<i class="fa-solid fa-water" title="Nile River View" id="NileRiverView"></i>
<i class="fa-solid fa-umbrella-beach" title="Large Terrace" id="LargeTerrace"></i>
<i class="fa-solid fa-water-ladder" title="Pool View" id="PoolView"></i>
<i class="fa-solid fa-wifi" title="WiFI" id="WiFI"></i>
<i class="fa-solid fa-tv" title="TV" id="TV"></i>
<i class="fa-solid fa-snowflake" title="Central Air Conditioning" id="CentralAirConditioning"></i>
<i class="fa-solid fa-bath" title="Bathtub" id="Bathtub"></i>
<i class="fa-solid fa-beer-mug-empty" title="Mini Bar" id="MiniBar"></i>
<i class="fa-solid fa-ban-smoking" title="Non-Smoking" id="Non-Smoking"></i>
<i class="fa-solid fa-mug-hot" title="Complimentary Tea & Coffee" id="Complimentary Tea & Coffee"></i>
<i class="fa-solid fa-utensils" title="24-Hour Room Service" id="24-HourRoomService"></i>
<i class="fa-solid fa-city" title="City View" id="CityView"></i>
<i class="fa-solid fa-cube" title="Additional Feature" id="AdditionalFeature"></i>
<i class="fa-solid fa-person" title="Number of people" id="Numberofpeople"></i>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= room.Title %>
    </title>
    <link rel="stylesheet" href="/CSS/room.css">
</head>

<body>
    <header class="room-header">
        <h1>
            <%= room.Title %>
        </h1>
    </header>

    <div class="room-details">
        <div class="room-image">
            <% const newImagePath=room.imageURL.substring(15); %>
                <img src="/IMAGES/ROOMS/<%= newImagePath %>" alt="<%= room.Title %>">
                <%
                const exe = ".jpeg"
                const more =  newImagePath.substring(0, newImagePath.length - exe.length);
                %>
                <img src="/IMAGES/ROOMS/<%= more%>1" alt="<%= room.Title %>">
                <img src="/IMAGES/ROOMS/<%= more%>2" alt="<%= room.Title %>">
        </div>
        <div class="room-description">
            <h2>Description</h2>
            <p>
                <%= room.description %>
            </p>
            <p>
                <% if (room.characteristics) { %>
                    <% console.log("Room Characteristics:", room.characteristics); %>
                        <% try { %>
                            <% const characteristicsArray=JSON.parse(room.characteristics.replace(/'/g, '"' )); %>
                                <% characteristicsArray.forEach(characteristic=> { %>
                                    <% const characteristicWithoutSpaces=characteristic.replace(/\s/g, '' ); %>
                                        <i class="fa-solid fa-<%=characteristicWithoutSpaces%>"
                                            title="<%= characteristic %>" id="<%=characteristicWithoutSpaces%>"></i>
                                        <% console.log(characteristicWithoutSpaces); %>
                                            <% }); %>
                                                <% } catch (error) { %>
                                                    <% console.log("Error parsing characteristics:", error); %>
                                                        <% } %>
                                                            <% } else { %>
                                                                <script>
                                                                    console.warn('Room characteristics is not defined or empty.');
                                                                </script>
                                                                <% } %>
            </p>






            <hr>
            <h2>Room Details</h2>
            <ul>
                <li>Quantity: <%= room.quantity %>
                </li>
                <li>Capacity: <%= room.capacity %> people</li>
                <li>Starting Price: $<%= room.startingPrice %> per night</li>
                <li>
                    <%= room.executive ? 'Executive Club Room' : 'Basma Hotel Room' %>
                </li>
            </ul>
            <hr>
            <h2>Book Now!</h2>
            <form action="/guest/reserve" method="POST">
                <input type="hidden" name="room" value="<%= room.Title %>">
                <label for="arrivalDate">Arrival Date:</label>
                <input type="date" id="arrivalDate" name="arrivalDate" required>
                <br>
                <label for="departureDate">Departure Date:</label>
                <input type="date" id="departureDate" name="departureDate" required>
                <br>
                <input type="submit" value="Book Now">
            </form>


        </div>

    </div>
</body>

</html>