<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
    <link rel="stylesheet" href="/CSS/dashboard.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Montserrat:300,600" />

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.1.3/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/jquery.selectric/1.10.1/selectric.css">
    <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/material-design-iconic-font/2.2.0/css/material-design-iconic-font.min.css">
    <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/material-design-iconic-font/2.2.0/css/material-design-iconic-font.min.css">
    <link rel="stylesheet" href="/CSS/guests.css">
</head>

<body>
    <%- include("./Partials/dashPartial.ejs") %>
        <main role="main" class="col-md-9 ml-sm-auto col-lg-10 my-3">
            <div class="container">
                <div class="title-strap">
                    <h2>Basma's Users</h2>
                </div>
                <div class="row justify-content-end mb-3">
                    <div class="col-auto">
                        <button class="btn btn-success" onclick="addGuestRow()">Add User</button>
                    </div>
                    <div class="col-auto" id="saveButtonContainer" style="display: none;">
                        <button id="saveButton" class="btn btn-primary" onclick="saveGuestRow()">Save</button>
                    </div>
                </div>
                <div class="table-responsive">
                    <table class="guests-table" id="guestTable">
                        <thead>
                            <tr>
                                <th>First Name</th>
                                <th>Last Name</th>
                                <th>Email</th>
                                <th>Password</th>
                                <th>Phone</th>
                                <th>Role</th>
                                <th>Bio</th>
                                <th>Address</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% users.forEach(function(guest) { %>
                                <tr>
                                    <form action="/admin/modifyUser" method="post">
                                    <td><input type="text" class="form-control" required name="firstName" value="<%= guest.firstName %>" readonly>
                                    </td>
                                    <td><input type="text" class="form-control" required name="lastName" value="<%= guest.lastName %>" readonly>
                                    </td>
                                    <td><input type="hidden" class="form-control" required name="email" value="<%= guest.email %>" readonly>
                                        <%= guest.email %>
                                    </td>
                                    <td>
                                        <input type="hidden">
                                        <div>CONFIDENTIAL</div>
                                    </td>
                                    <td><input type="number" class="form-control" required name="phoneNumber" value="<%= guest.phoneNumber %>"
                                            readonly>
                                    </td>
                                    <td><input type="text" class="form-control" required name="role" value="<%= guest.role %>" readonly>
                                    </td>
                                    <td><input type="text" class="form-control" required name="bio" value="<%= guest.bio %>" readonly>
                                    </td>
                                    <td><input type="text" class="form-control" required name="address" value="<%= guest.address %>" readonly>
                                    </td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <button class="btn-success" style="margin-right: 10px; display: none;"
                                               type="submit">Save</button>
                                            </form>
                                            <button class="btn btn-primary" style="margin-right: 10px;"
                                                onclick="toggleReadOnly(this)">Edit</button>
                                                <form method="post" action="/admin/removeUser" >
                                                    <input type="hidden" name="email" value="<%= guest.email %>" >
                                                    <button class="btn btn-danger" type="submit">Delete</button>
                                                </form>
                                        </div>
                                    </td>
                                </tr>
                                <% }); %>
                        </tbody>
                    </table>
                    <div id="errorMessage" style="color: red;"></div>
                </div>
            </div>
        </main>
        </div>
        </div>
        <script src="/JS/users.js"></script>
</body>

</html>